generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
}

model User {
  id    String @id @default(uuid()) @map("_id")
  email String @unique
  date  DateTime @db.Date
}
